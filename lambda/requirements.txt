# Lambda function dependencies
# Note: boto3 and botocore are already available in the Lambda runtime
# Compatible with Python 3.9+ (Lambda uses 3.9, local dev can use newer versions)

# Development and testing dependencies
pytest>=7.0.0,<9.0.0
moto>=4.0.0,<5.0.0
boto3>=1.26.0
botocore>=1.29.0

# Code quality tools
black>=22.0.0
flake8>=5.0.0
pylint>=2.15.0

# Type checking
mypy>=1.0.0

# Security scanning
bandit>=1.7.0
safety>=2.0.0